package spring_orm;
/*
 * -------Spring ORM(Object relational mapping)-------------
 * Spring with ORM Frameworks
 * ORM frameworks->Hibernate,Toplink,iBATIS(called ORM tools)
 * --------Advantage of Using Spring ORM-----------
 * Less coding is required.(spring+ORM).
 * Easy to test.
 * Better exception handling(Spring will manage it by default)
 * still you can use your own try catch.
 * Integrated Transaction Management.
 * --------Working with database-------------
 * Java program(object)->JDBC->Database(JDBC APPROACH-Traditional)
 * Java program(Object)->ORM->Database
 * here we will create a class in java program and we will have to map it with the database
 and define its all field like name ,college(Attribute name)-ORM APPROACH
 *How to work with Spring ORM
 *HibernateTemplate-Save,Update,Insert,Get,loadAll
 *First of all we will need ProductDao class,EmployeeDao class,StudentDao class etc
 *in Dao class we will use the object of HibernateTemplate class.
 *Hibernate Template depends on SessionFactory.
 *SessionFactory is an interface(LocalSessionFactoryBean is a class )
 *we will give the object of it to hibernatetemplate class
 *DataSource object create and give all properties(like url,username,Password).
 *Hibernate Properties
 *Annotatedclass we will have to provide these 3 things to LocalSessionFactory
 *-------------------Spring ORM Complete setup----------------------
 *-------------------Adding Configuration file---------------------
 *-------------------Complete configuration of spring orm--------------
 *-------------------Console application using ORM(CRUD Operation)----------
 *
 *
 * 
 * 
 */
public class Intoduction_to_Spring_ORM {

}
